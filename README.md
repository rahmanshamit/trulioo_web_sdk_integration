# trulioo_web_sdk_integration
Example use-case of Trulioo's web SDK, do implement a document verification workflow from desktop -> mobile -> back to desktop.
Use's Trulioo's web SDK to initialize a document verification process, where a user can upload picture of their identity verification document and selfie from their phones,
and the web application is notified using callbacks to update on the desktop.
